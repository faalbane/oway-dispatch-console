DATABASE_URL="file:./dev.db"
PORT=3001

# Optional: set to enable Google Maps Geocoding API for address verification
# on the New Shipment form. If absent, the app falls back to Nominatim (OSM),
# which also works fine — just lower-quality address resolution.
# GOOGLE_MAPS_API_KEY=your-key-here
